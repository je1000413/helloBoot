<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mice.backoffice.c2v_mice_backoffice_api.repository.mybatis.TestRepository">

    <select id="getCodeList" resultType="CodeEntity">
        CALL sp_sys_code_info_select(#{in_cd_typ}, #{in_cd})
    </select>

    <select id="getCodeListTest" resultMap="result1,result2,result3">
        CALL sp_sys_code_info_select_test()
    </select>

    <resultMap id="result1" type="CodeEntity"></resultMap>
    <resultMap id="result2" type="CodeEntity"></resultMap>
    <resultMap id="result3" type="CodeEntity"></resultMap>

    <select id="adminRoleCheck" resultType="Integer">
        CALL sp_platform_admin_menu_select_for_role_check(#{in_cur_admin_id}, #{in_cur_menu_seq}, #{in_method})
    </select>

</mapper>